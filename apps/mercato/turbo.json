{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "cache": true,
      "inputs": ["$TURBO_DEFAULT$", ".mercato/generated/**"],
      "outputs": [".mercato/next/**", "!.mercato/next/cache/**"]
    }
  }
}
