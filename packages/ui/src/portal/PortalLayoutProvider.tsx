/**
 * @deprecated Use PortalLayoutShell instead — it receives server-resolved data
 * from the layout server component, eliminating client-side loading states.
 */
export { PortalLayoutShell as PortalLayoutProvider } from './PortalLayoutShell'
