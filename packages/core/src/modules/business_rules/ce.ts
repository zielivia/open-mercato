/**
 * Custom Entity definitions for Business Rules module
 *
 * This file declares custom entities that can be extended with custom fields
 * System entities (from ORM) are auto-discovered - this is for labeling/field seeding
 */

// Will be populated as entities are created
const systemEntities: any[] = []

export const entities = systemEntities
export default systemEntities
