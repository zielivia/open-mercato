"use client"

/**
 * Deprecated debug helper.
 *
 * Internal DOM bridge events must not be shown to end users as flash
 * messages. Keep this component as a no-op so any legacy imports remain safe.
 */
export function SseEventIndicator(): null {
  return null
}
