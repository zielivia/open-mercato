---
id: overview
title: CLI Overview
sidebar_label: Overview
description: Command line tooling for automating setup, database maintenance, and module operations in Open Mercato.
---

Open Mercato bundles CLI commands for project setup, tenant administration, and module scaffolding. Top-level helpers are exposed as Yarn scripts (`yarn initialize`, `yarn db:generate`, …) while module-specific utilities live under the umbrella command:

```bash
yarn mercato <module> <command> [options]
```

Run `yarn mercato --help` to list modules and `yarn mercato <module> --help` to drill in.

## Workflow Cheat Sheet

- **Bootstrap & Database**
  - [`yarn initialize`](./init) – full app bootstrap with optional reinstall mode (wraps `mercato init`).
  - [`yarn db:generate`](./db-generate) – per-module migration diffing (alias for `mercato db generate`).
  - [`yarn db:migrate`](./db-migrate) – apply migrations and refresh generated metadata.
  - [`yarn db:greenfield`](./db-greenfield) – destructive reset that regenerates a clean schema.
- **Development Runtime**
  - [`yarn dev:ephemeral`](./dev-ephemeral) – start a dev server on a free port, open browser automatically, and register active instances in `.ai/dev-ephemeral-envs.json`.
- **Code Generation**
  - `mercato generate` or `mercato generate all` – run all generators (entity IDs, registry, entities, DI, API client).
  - `mercato generate entity-ids` – generate entity ID mappings.
  - `mercato generate registry` – generate module registry.
  - `mercato generate entities` – generate ORM entity aggregation.
  - `mercato generate di` – generate dependency injection registrars.
  - `mercato generate api-client` – generate typed API client.
- **Database**
  - `mercato db generate` – generate per-module migrations.
  - `mercato db migrate` – apply pending migrations.
  - `mercato db greenfield --yes` – destructive reset and regenerate schema.
- **Auth module**
  - [`mercato auth seed-roles`](./auth-seed-roles)
  - [`mercato auth setup`](./auth-setup)
  - [`mercato auth add-user`](./auth-add-user)
  - [`mercato auth set-password`](./auth-set-password)
  - [`mercato auth list-orgs`](./auth-list-orgs)
  - [`mercato auth list-users`](./auth-list-users)
  - [`mercato auth list-tenants`](./auth-list-tenants)
- **Security**
  - [`mercato api_keys add`](./api-keys) – mint scoped API keys for automations.
- **Events & Queue**
  - `mercato events emit <event> [payload] [-p]` – emit an event (optionally persistent).
  - `mercato events clear` – clear the events queue.
  - `mercato queue worker <name>` – run a queue worker (e.g., `queue worker events`).
  - `mercato queue status <name>` – show queue job counts.
  - `mercato queue clear <name>` – clear all jobs from a queue.
- **Scaffolding**
  - `mercato scaffold module <name>` – create a new module skeleton.
  - `mercato scaffold entity` – interactive entity creation wizard.
  - `mercato scaffold crud <module> <Entity> [route]` – generate CRUD route for an entity.
- **Entities**
  - [`mercato entities install`](./entities-install)
  - [`mercato entities decrypt-database`](./entities-decrypt-database)
- **Module Ejection**
  - [`mercato eject --list`](./eject) – list modules available for ejection.
  - [`mercato eject <moduleId>`](./eject) – copy a core module into your local `src/modules/` for full customization.
- **Integration Testing**
  - [`yarn test:integration:ephemeral`](./test-integration) – run Playwright tests in disposable app+DB containers.
  - [`yarn test:integration:ephemeral:interactive`](./test-integration) – keep one disposable app+DB running and choose tests from an interactive menu.
  - [`yarn test:integration:ephemeral:start`](./test-ephemeral) – start disposable app+DB only for QA/MCP exploration.

Each linked page provides usage syntax, option breakdowns, sample output, and troubleshooting notes so you can copy commands directly into your terminal.
